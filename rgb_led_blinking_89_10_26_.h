  
 //**************************************************************************************************************
 
Rblinking(int on,int off)
{
 PORTB=0x02;
 delay_ms(on);
 PORTB=0x00;
 delay_ms(off);

}
  
 //**************************************************************************************************************

Gblinking(int on,int off)
{
 PORTB=0x04;
 delay_ms(on);
 PORTB=0x00;
 delay_ms(off);

}

 //**************************************************************************************************************

Bblinking(int on,int off)
{
 PORTB=0x08;
 delay_ms(on);
 PORTB=0x00;
 delay_ms(off);

}


 //**************************************************************************************************************

RGblinking(int on,int off)
{

 PORTB=0x06;
 delay_ms(on);
 PORTB=0x00;
 delay_ms(off);

}


 //**************************************************************************************************************


GBblinking(int on,int off)
{
 
 PORTB=0x0C;
 delay_ms(on);
 PORTB=0x00;
 delay_ms(off);

}


 //**************************************************************************************************************

RBblinking(int on,int off)
{

 PORTB=0x0A;
 delay_ms(on);
 PORTB=0x00;
 delay_ms(off);

}


 //**************************************************************************************************************

RGBblinking(int on,int off)
{
 
 PORTB=0x0E;
 delay_ms(on);
 PORTB=0x00;
 delay_ms(off);

}


 //**************************************************************************************************************